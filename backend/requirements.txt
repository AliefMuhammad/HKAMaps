fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
opencv-python-headless==4.12.0.88
numpy>=1.26.0
ultralytics>=8.3.0
aiofiles>=23.2.0
huggingface_hub>=0.24.0
Pillow>=10.0.0

# Zero-shot asset detection (YOLO-World) — already included via ultralytics>=8.3.0
# ultralytics.YOLOWorld is bundled — no extra install needed.
